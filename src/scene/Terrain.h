
#include "vulkan/Mesh.h"

/*
/// Terrains are meshes where the vertices are arranged in a grid. Only the height really matters
class Terrain : public Mesh {
private:
	uint32_t width, height;

public:
	Terrain(VulkanApplication &app, const std::string path, const uint32_t width, const uint32_t height) :
			Mesh(app, path), width(width), height(height){};
	~Terrain() = default;

	float get_height(const float x, const float y);
};
*/
